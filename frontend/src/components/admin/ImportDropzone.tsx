import { ChangeEvent, DragEvent, useState } from 'react';
export default function ImportDropzone({ onFile, accept = '.xlsx,.xls,.csv' }: { onFile: (file: File) => void; accept?: string }) { const [drag, setDrag] = useState(false); const handle = (file?: File) => { if (file) onFile(file); }; return <label onDragOver={(event) => { event.preventDefault(); setDrag(true); }} onDragLeave={() => setDrag(false)} onDrop={(event: DragEvent<HTMLLabelElement>) => { event.preventDefault(); setDrag(false); handle(event.dataTransfer.files[0]); }} className={`block cursor-pointer rounded-xl border-2 border-dashed p-8 text-center ${drag ? 'border-blue-500 bg-blue-50' : 'border-slate-300'}`}><input type="file" className="sr-only" accept={accept} onChange={(event: ChangeEvent<HTMLInputElement>) => handle(event.target.files?.[0])} /><p className="font-semibold">Kéo thả file vào đây</p><p className="mt-1 text-sm text-slate-500">hoặc bấm để chọn Excel/CSV</p></label>; }