import Badge from '../ui/Badge'; import { EVENT_STATUS_LABELS } from '@/constants/ui'; export default function EventStatusBadge({ status }: { status: string }) { const tone = status === 'REGISTRATION_OPEN' || status === 'EVENT_ACTIVE' ? 'success' : status.includes('IN_PROGRESS') ? 'warning' : status === 'EVENT_COMPLETED' ? 'neutral' : 'info'; return <Badge tone={tone}>{EVENT_STATUS_LABELS[status] || status}</Badge>; }