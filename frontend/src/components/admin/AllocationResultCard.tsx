import type { ReactNode } from 'react'; export default function AllocationResultCard({ title, total, allocated, unallocated, warnings, action }: { title: string; total: number; allocated: number; unallocated: number; warnings?: string[]; action?: ReactNode }) { return <section className="card p-5"><div className="flex flex-wrap justify-between gap-3"><h3 className="font-semibold">{title}</h3>{action}</div><div className="mt-4 grid grid-cols-3 gap-3 text-center"><div><p className="text-xs text-slate-500">Tổng</p><p className="text-xl font-bold">{total}</p></div><div><p className="text-xs text-slate-500">Đã xếp</p><p className="text-xl font-bold text-emerald-600">{allocated}</p></div><div><p className="text-xs text-slate-500">Chưa xếp</p><p className="text-xl font-bold text-red-600">{unallocated}</p></div></div>{warnings?.length ? <ul className="mt-4 list-disc pl-5 text-sm text-amber-700">{warnings.map((warning) => <li key={warning}>{warning}</li>)}</ul> : null}</section>; }