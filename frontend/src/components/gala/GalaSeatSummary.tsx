import Button from '../ui/Button';
export default function GalaSeatSummary({ selectedCount, maxCount, onLock, onConfirm, onRelease, loading }: { selectedCount: number; maxCount: number; onLock?: () => void; onConfirm?: () => void; onRelease?: () => void; loading?: boolean }) { return <div className="sticky bottom-3 rounded-xl border border-slate-200 bg-white p-4 shadow-lg"><div className="flex flex-wrap items-center justify-between gap-3"><p className="text-sm text-slate-600">Đã chọn <strong>{selectedCount}</strong>/{maxCount} ghế</p><div className="flex flex-wrap gap-2">{onRelease && <Button variant="secondary" onClick={onRelease}>Bỏ chọn</Button>}{onLock && <Button loading={loading} disabled={!selectedCount} onClick={onLock}>Giữ ghế</Button>}{onConfirm && <Button loading={loading} disabled={!selectedCount} onClick={onConfirm}>Xác nhận</Button>}</div></div></div>; }