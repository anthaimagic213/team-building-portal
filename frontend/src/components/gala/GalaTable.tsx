import type { GalaSeatResponse } from '@/types/gala'; import GalaSeat from './GalaSeat';
export default function GalaTable({ tableCode, seats, selected, onToggle }: { tableCode: string; seats: GalaSeatResponse[]; selected: string[]; onToggle: (code: string) => void }) { return <section className="rounded-xl border border-slate-200 p-4"><h3 className="mb-3 text-center text-sm font-semibold">Bàn {tableCode}</h3><div className="grid grid-cols-2 gap-2">{seats.map((seat) => <GalaSeat key={seat.id} code={seat.seat_code} status={seat.status} selected={selected.includes(seat.seat_code)} onClick={() => onToggle(seat.seat_code)} />)}</div></section>; }