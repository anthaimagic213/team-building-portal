import type { SeatStatus } from '@/types/gala';
export default function GalaSeat({ code, status, selected, onClick }: { code: string; status: SeatStatus; selected?: boolean; onClick?: () => void }) { const disabled = status !== 'AVAILABLE' && !selected; return <button type="button" disabled={disabled} onClick={onClick} aria-label={`Ghế ${code}, ${status}`} className={`min-h-12 rounded-lg border p-2 text-xs font-bold transition ${selected ? 'border-blue-700 bg-blue-600 text-white' : status === 'AVAILABLE' ? 'border-emerald-200 bg-emerald-50 text-emerald-700 hover:bg-emerald-100' : status === 'CONFIRMED' ? 'border-blue-200 bg-blue-100 text-blue-700' : 'cursor-not-allowed border-slate-200 bg-slate-100 text-slate-400'}`}>{code}</button>; }