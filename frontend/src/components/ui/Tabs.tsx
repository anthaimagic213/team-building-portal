export interface TabItem { id: string; label: string; }
export default function Tabs({ items, activeId, onChange }: { items: TabItem[]; activeId: string; onChange: (id: string) => void }) { return <div className="flex gap-1 overflow-x-auto border-b border-slate-200">{items.map((item) => <button key={item.id} className={`min-h-11 whitespace-nowrap border-b-2 px-4 text-sm font-medium ${item.id === activeId ? 'border-blue-600 text-blue-600' : 'border-transparent text-slate-500 hover:text-slate-900'}`} onClick={() => onChange(item.id)} aria-selected={item.id === activeId}>{item.label}</button>)}</div>; }