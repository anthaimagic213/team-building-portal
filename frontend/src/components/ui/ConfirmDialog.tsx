import Button from './Button'; import Modal from './Modal';
export default function ConfirmDialog({ open, title = 'Xác nhận thao tác', message, confirmLabel = 'Xác nhận', onConfirm, onClose, loading }: { open: boolean; title?: string; message: string; confirmLabel?: string; onConfirm: () => void; onClose: () => void; loading?: boolean }) { return <Modal open={open} title={title} onClose={onClose}><p className="text-sm leading-6 text-slate-600">{message}</p><div className="mt-6 flex justify-end gap-3"><Button variant="secondary" onClick={onClose}>Hủy</Button><Button variant="danger" loading={loading} onClick={onConfirm}>{confirmLabel}</Button></div></Modal>; }