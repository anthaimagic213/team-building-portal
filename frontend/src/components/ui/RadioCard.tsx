import { InputHTMLAttributes } from 'react';
export default function RadioCard({ title, description, checked, ...props }: InputHTMLAttributes<HTMLInputElement> & { title: string; description?: string }) { return <label className={`block cursor-pointer rounded-xl border p-4 transition ${checked ? 'border-blue-600 bg-blue-50 ring-1 ring-blue-600' : 'border-slate-200 hover:border-blue-300'}`}><input type="radio" className="sr-only" checked={checked} {...props} /><span className="font-semibold text-slate-900">{title}</span>{description && <span className="mt-1 block text-sm text-slate-500">{description}</span>}</label>; }