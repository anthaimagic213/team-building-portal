import { NavLink } from 'react-router-dom';
import { ADMIN_NAV_ITEMS, USER_NAV_ITEMS } from '@/constants/routes';
export default function Sidebar({ admin = false, representative = false }: { admin?: boolean; representative?: boolean }) { const items = admin ? ADMIN_NAV_ITEMS : USER_NAV_ITEMS.filter((item) => !item.representativeOnly || representative); return <aside className={`hidden shrink-0 p-4 md:block ${admin ? 'w-64 bg-slate-900' : 'w-60 border-r border-slate-200 bg-white'}`}><nav className="space-y-1">{items.map((item) => <NavLink key={item.to} to={item.to} end={item.to === (admin ? '/admin' : '/journey')} className={({ isActive }) => `flex min-h-11 items-center rounded-lg px-3 text-sm font-medium ${isActive ? (admin ? 'bg-blue-600 text-white' : 'bg-blue-50 text-blue-700') : admin ? 'text-slate-300 hover:bg-slate-800' : 'text-slate-600 hover:bg-slate-50'}`}>{item.label}</NavLink>)}</nav></aside>; }