import { NavLink } from 'react-router-dom';
export default function MobileMenu({ open, onClose, items }: { open: boolean; onClose: () => void; items: Array<{ to: string; label: string }> }) { if (!open) return null; return <div className="fixed inset-0 z-40 md:hidden"><button className="absolute inset-0 bg-slate-900/40" onClick={onClose} aria-label="Đóng menu" /><nav className="relative h-full w-72 bg-white p-5 shadow-xl">{items.map((item) => <NavLink key={item.to} onClick={onClose} to={item.to} className="flex min-h-11 items-center border-b border-slate-100 text-sm font-medium text-slate-700">{item.label}</NavLink>)}</nav></div>; }