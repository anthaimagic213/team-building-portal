import { Link } from 'react-router-dom';
export default function Header({ title = 'Team Building Portal', userName, onMenu }: { title?: string; userName?: string; onMenu?: () => void }) { return <header className="sticky top-0 z-20 border-b border-slate-200 bg-white/95 backdrop-blur"><div className="flex h-16 items-center justify-between px-4 sm:px-6"><div className="flex items-center gap-3"><button className="min-h-11 min-w-11 rounded-lg text-xl hover:bg-slate-100 md:hidden" onClick={onMenu} aria-label="Mở menu">☰</button><Link to="/journey" className="font-bold text-slate-900">{title}</Link></div>{userName && <span className="hidden text-sm text-slate-600 sm:inline">{userName}</span>}</div></header>; }