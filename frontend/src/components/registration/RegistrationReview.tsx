import type { RegistrationBase } from '@/types/registration';
export default function RegistrationReview({ data }: { data: RegistrationBase }) { return <section className="rounded-xl bg-slate-50 p-4"><h3 className="font-semibold">Tóm tắt đăng ký</h3><p className="mt-2 text-sm">{data.is_participating ? 'Có tham gia' : 'Không tham gia'} · {data.desired_shift || 'Chưa chọn ca'}</p><p className="mt-1 text-sm text-slate-600">Xe: {[1, 2, 3, 4].filter((number) => Boolean(data[`needs_vehicle_route_${number}` as keyof RegistrationBase])).map((number) => `Route ${number}`).join(', ') || 'Không có'}</p></section>; }