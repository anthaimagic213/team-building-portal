export default function ParticipationSelector({ value, onChange, disabled }: { value: boolean; onChange: (value: boolean) => void; disabled?: boolean }) { return <fieldset disabled={disabled}><legend className="form-label">Bạn có tham gia không?</legend><div className="flex gap-4"><label className="flex items-center gap-2"><input type="radio" checked={value} onChange={() => onChange(true)} /> Có</label><label className="flex items-center gap-2"><input type="radio" checked={!value} onChange={() => onChange(false)} /> Không</label></div></fieldset>; }