import { VEHICLE_ROUTES } from '@/constants/ui'; import Checkbox from '../ui/Checkbox';
export default function VehicleRouteCard({ routeNumber, checked, onChange, disabled }: { routeNumber: 1 | 2 | 3 | 4; checked: boolean; onChange: (value: boolean) => void; disabled?: boolean }) { const route = VEHICLE_ROUTES.find((item) => item.number === routeNumber)!; return <div className="rounded-xl border border-slate-200 p-4"><Checkbox label={route.label} checked={checked} disabled={disabled} onChange={(event) => onChange(event.target.checked)} /><p className="mt-2 text-xs text-slate-500">Route {route.number}</p></div>; }