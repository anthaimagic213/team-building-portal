import { useState } from 'react'; import Modal from '../ui/Modal'; import Button from '../ui/Button';
export default function CancelVehicleDialog({ open, routeNumber, onClose, onConfirm, loading }: { open: boolean; routeNumber: number; onClose: () => void; onConfirm: (reason: string) => void; loading?: boolean }) { const [reason, setReason] = useState(''); return <Modal open={open} title={`Hủy xe Route ${routeNumber}`} onClose={onClose}><p className="text-sm text-slate-600">Thao tác có thể không hoàn tác và không chạy lại Auto Allocation.</p><textarea className="input mt-4" placeholder="Lý do hủy (không bắt buộc)" value={reason} onChange={(event) => setReason(event.target.value)} /><div className="mt-5 flex justify-end gap-2"><Button variant="secondary" onClick={onClose}>Đóng</Button><Button variant="danger" loading={loading} onClick={() => onConfirm(reason)}>Xác nhận hủy</Button></div></Modal>; }