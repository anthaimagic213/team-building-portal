import type { DesiredShift } from '@/types/registration'; import RadioCard from '../ui/RadioCard';
export default function ShiftSelector({ value, onChange, disabled }: { value: DesiredShift | null; onChange: (value: DesiredShift) => void; disabled?: boolean }) { return <fieldset disabled={disabled}><legend className="form-label">Ca bay mong muốn</legend><div className="grid gap-3 sm:grid-cols-2"><RadioCard title="Ca 1" description="Trước 17:00" value="SHIFT_1" checked={value === 'SHIFT_1'} onChange={() => onChange('SHIFT_1')} /><RadioCard title="Ca 2" description="Sau 17:00" value="SHIFT_2" checked={value === 'SHIFT_2'} onChange={() => onChange('SHIFT_2')} /></div><p className="mt-2 text-xs text-slate-500">Đây là nguyện vọng, không phải kết quả cam kết.</p></fieldset>; }