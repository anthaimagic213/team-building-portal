import type { EventScheduleItem } from '@/types/journey';
export default function JourneyTimeline({ items }: { items: EventScheduleItem[] }) { return <ol className="space-y-5">{items.map((item) => <li className="relative border-l-2 border-blue-200 pl-5" key={`${item.time}-${item.title}`}><span className="absolute -left-2 top-1 h-3 w-3 rounded-full bg-blue-600" /><p className="font-semibold">{item.title}</p><p className="text-sm text-slate-500">{new Date(item.time).toLocaleString('vi-VN')}</p>{item.location && <p className="text-sm text-slate-600">{item.location}</p>}</li>)}</ol>; }