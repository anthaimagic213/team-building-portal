import type { NotificationItem } from '@/types/journey';
export default function NotificationList({ items, onRead }: { items: NotificationItem[]; onRead?: (id: string) => void }) { return <div className="space-y-3">{items.map((item) => <article className={`rounded-lg border p-4 ${item.is_read ? 'border-slate-200' : 'border-blue-200 bg-blue-50'}`} key={item.id}><div className="flex justify-between gap-3"><h3 className="font-semibold">{item.title}</h3><span className="status-neutral">{item.notification_type}</span></div><p className="mt-2 text-sm text-slate-600">{item.content}</p>{!item.is_read && onRead && <button className="mt-3 text-sm font-semibold text-blue-600" onClick={() => onRead(item.id)}>Đánh dấu đã đọc</button>}</article>)}</div>; }