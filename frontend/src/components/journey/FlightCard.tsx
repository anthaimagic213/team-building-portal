import type { JourneyFlightInfo } from '@/types/journey';
export default function FlightCard({ flight }: { flight: JourneyFlightInfo }) { return <article className="card p-5"><div className="flex items-center justify-between"><h3 className="font-semibold">{flight.direction === 'OUTBOUND' ? 'Chuyến bay đi' : 'Chuyến bay về'}</h3><span className="status-info">{flight.flight_code}</span></div><div className="mt-4 grid grid-cols-2 gap-4 text-sm"><div><p className="text-slate-500">Đi</p><p className="font-medium">{flight.origin}</p><p>{new Date(flight.departure_time).toLocaleString('vi-VN')}</p></div><div><p className="text-slate-500">Đến</p><p className="font-medium">{flight.destination}</p><p>{new Date(flight.arrival_time).toLocaleString('vi-VN')}</p></div></div></article>; }