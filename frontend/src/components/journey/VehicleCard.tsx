import type { JourneyVehicleInfo } from '@/types/journey';
export default function VehicleCard({ vehicle }: { vehicle: JourneyVehicleInfo }) { return <article className="card p-5"><h3 className="font-semibold">{vehicle.vehicle_name}</h3><p className="mt-1 text-sm text-blue-600">{vehicle.route_type}</p><dl className="mt-4 space-y-2 text-sm"><div><dt className="text-slate-500">Khởi hành</dt><dd>{new Date(vehicle.departure_time).toLocaleString('vi-VN')}</dd></div><div><dt className="text-slate-500">Lộ trình</dt><dd>{vehicle.pickup_location} → {vehicle.dropoff_location || 'Chưa cập nhật'}</dd></div></dl>{vehicle.captain_phone && <a className="mt-4 inline-block text-sm font-semibold text-blue-600" href={`tel:${vehicle.captain_phone}`}>Gọi trưởng xe</a>}</article>; }